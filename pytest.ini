[pytest]
testpaths = tests
markers =
    integration: tests that call real LLM APIs (deselected by default)
    eval: end-to-end scenario evals that call the real planner + response LLMs (deselected by default)
addopts = -m "not integration and not eval" -v
